# Shared dependencies — covers both training and attack scripts.
# The Docker image uses target/requirements.txt (inference-only, no attack tools).

--extra-index-url https://download.pytorch.org/whl/cpu

# Deep learning — model training and adversarial example crafting
torch==2.4.1+cpu
torchvision==0.19.1+cpu

# API server — needed to run the target locally (outside Docker)
fastapi
uvicorn[standard]
python-multipart

# HTTP clients — sending crafted requests to the target API
requests
httpx

# Image handling — loading/saving images for adversarial input crafting
pillow

# Numerical computing — adversarial perturbations and data handling
numpy
pandas

# ML utilities — metrics, baseline classifiers
scikit-learn

# Plotting — visualising attack results and perturbation magnitudes
matplotlib
